/**
 * Tailwind v4 does not read this file.
 *
 * This project builds CSS through `@tailwindcss/vite`, where configuration is
 * CSS-first: design tokens are declared in `src/index.css` inside `@theme
 * inline`, and class-based dark mode is declared there with `@custom-variant`.
 *
 * Edit `src/index.css` — not this file. It is kept only so tooling that probes
 * for a Tailwind config finds an explanation rather than nothing.
 */
export default {};
